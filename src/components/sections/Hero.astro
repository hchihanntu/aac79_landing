---
import Container from "@/components/ui/Container.astro";
import Button from "@/components/ui/Button.astro";
import Icon from "@/components/ui/Icon.astro";
import YouTubeEmbed from "@/components/ui/YouTubeEmbed.astro";

const benefits = [
  "Nhẹ hơn – giảm tải trọng công trình",
  "Nhanh hơn – rút ngắn tiến độ thi công",
  "Đồng bộ hơn – đầy đủ giải pháp vật liệu",
];
---

<section id="hero" class="relative flex min-h-screen items-center overflow-hidden pt-32 pb-16">
  <div
    class="absolute inset-0 -z-20 bg-cover bg-center"
    style="background-image: linear-gradient(112deg, rgba(6,32,20,0.84) 0%, rgba(11,94,60,0.74) 50%, rgba(8,39,25,0.58) 100%), url('/images/hero-bg.webp');"
  >
  </div>

  <Container class="relative grid grid-cols-1 gap-9 lg:grid-cols-12 lg:items-center">
    <!-- Cột trái: nội dung copy (chiếm trọng số thị giác lớn hơn — 7/12) -->
    <div class="lg:col-span-7">
      <h1 data-aos="fade-up" data-aos-delay="80" class="text-balance text-[clamp(2rem,4vw,3rem)] font-bold uppercase leading-tight text-white">
        Giải pháp <span class="text-brand-vivid">đồng bộ</span> cho công trình hiện đại
      </h1>

      <p data-aos="fade-up" data-aos-delay="140" class="mt-4 max-w-lg text-base leading-relaxed text-white/85 sm:text-lg">
        Chúng tôi chuyên phân phối gạch E-Block, tấm E-Panel, vữa chuyên dụng và thi công công trình sử dụng bê tông khí chưng áp tại Khánh Hòa và khu vực Nam Trung Bộ.
      </p>

      <div data-aos="fade-up" data-aos-delay="180" class="mt-5 grid gap-2">
        {benefits.map((b) => (
          <div class="flex items-start gap-3 text-base font-semibold text-white">
            <span class="mt-0.5 flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-brand-vivid/20 text-brand-vivid">
              <Icon name="check" class="h-3.5 w-3.5" />
            </span>
            {b}
          </div>
        ))}
      </div>

      <div data-aos="fade-up" data-aos-delay="220" class="mt-7">
        <Button href="/bang-gia/" variant="orange" size="lg">
          <Icon name="banknotes" class="h-7 w-7" />
          Xem bảng giá mới nhất
        </Button>
      </div>

      <div data-aos="fade-up" data-aos-delay="260" class="mt-3 flex flex-col items-start gap-3">
        <Button href="tel:0907523113" variant="white" size="lg" class="justify-start! text-left whitespace-nowrap">
          <Icon name="headset" class="h-7 w-7" />
          Tư vấn: 0907 523 113
        </Button>
      </div>
    </div>

    <!-- Cột phải: video (đối trọng bất đối xứng — 5/12) -->
    <div data-aos="zoom-in" data-aos-delay="200" class="lg:col-span-5">
      <div class="mb-5 grid grid-cols-2 gap-3">
        <div class="rounded-2xl border border-white/10 bg-white/8 px-5 py-4 text-center backdrop-blur">
          <strong class="block font-heading text-4xl font-bold leading-none text-brand-vivid sm:text-5xl">
            <span data-counter data-target="5">0</span>+
          </strong>
          <span class="mt-2 block text-sm font-medium text-white/75">Năm kinh nghiệm</span>
        </div>
        <div class="rounded-2xl border border-white/10 bg-white/8 px-5 py-4 text-center backdrop-blur">
          <strong class="block font-heading text-4xl font-bold leading-none text-brand-vivid sm:text-5xl">
            <span data-counter data-target="50">0</span>+
          </strong>
          <span class="mt-2 block text-sm font-medium text-white/75">Dự án hoàn thành</span>
        </div>
      </div>
      <YouTubeEmbed videoId="hfAxcYRgT18" title="AAC79 PLUS - Video giới thiệu" />
      <p class="mt-3 text-center text-sm text-white/60">
        Video giới thiệu thực tế công trình AAC79 PLUS
      </p>
    </div>
  </Container>
</section>

<script>
  const counters = document.querySelectorAll<HTMLElement>("[data-counter]");

  const animateCounter = (counter: HTMLElement) => {
    const target = Number(counter.dataset.target);
    const duration = 1200;
    const start = performance.now();

    const update = (now: number) => {
      const progress = Math.min((now - start) / duration, 1);
      const eased = 1 - Math.pow(1 - progress, 3);
      counter.textContent = String(Math.round(target * eased));

      if (progress < 1) requestAnimationFrame(update);
    };

    requestAnimationFrame(update);
  };

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (!entry.isIntersecting) return;
        animateCounter(entry.target as HTMLElement);
        observer.unobserve(entry.target);
      });
    },
    { threshold: 0.6 },
  );

  counters.forEach((counter) => observer.observe(counter));
</script>
