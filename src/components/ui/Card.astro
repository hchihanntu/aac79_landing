---
const { class: className = "", hover = true } = Astro.props;

const hoverClasses = hover ? "transition-colors duration-200 hover:border-brand-light" : "";
---

<div
  class={`rounded-2xl border border-ink-200 bg-white p-6 ${hoverClasses} ${className}`}
>
  <slot />
</div>
